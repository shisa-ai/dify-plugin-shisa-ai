[project]
name = "dify-shisa-ai"
version = "1.0.3"
description = "Shisa AI model provider plugin for Dify"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin==0.10.0",
    "httpx==0.28.1",
]

[dependency-groups]
dev = [
    "pip-audit==2.10.1",
    "pyyaml==6.0.3",
]
